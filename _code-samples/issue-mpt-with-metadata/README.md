# Issue an MPT with Metadata

Shows how to issue a Multi-Purpose Token (MPT) with metadata encoded according to the XLS-89 schema, then update its mutable properties and declare a property immutable, as defined by XLS-94D.
