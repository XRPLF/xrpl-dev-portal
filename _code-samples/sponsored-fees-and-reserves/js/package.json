{
  "name": "sponsored-fees-and-reserves-examples",
  "description": "Example code for sponsoring transaction fees and reserves on the XRP Ledger.",
  "dependencies": {
    "xrpl": "^5.0.0"
  },
  "type": "module",
  "scripts": {
    "build-local-xrpl": "cd \"$XRPL_JS\" && npm install && cd packages/xrpl && npm run build:lib && npm link",
    "use-local-xrpl": "npm link xrpl"
  }
}
